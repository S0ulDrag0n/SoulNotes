# Models Directory

This directory is reserved for any future model files needed by the application.

Currently, the VAD (Voice Activity Detection) system uses an energy-based detection
algorithm that requires no external model files. See `src/lib/vad-service.ts` for
implementation details.